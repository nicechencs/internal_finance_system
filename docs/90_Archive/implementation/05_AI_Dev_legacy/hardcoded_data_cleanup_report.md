# 硬编码数据清理报告

## 执行时间
2026-03-14

## 问题描述
代码中存在大量硬编码的演示数据，违反了最佳实践。演示数据应该通过独立的脚本或工具导入，而不是硬编码在应用程序代码中。

## 深度排查结果

### 后端代码

#### 1. 已清理的硬编码数据 ✅

**文件**：`backend/FinanceApp.Infrastructure/Data/DbInitializer.cs`

**清理内容**：
- 删除了 `SeedDemoDataAsync` 方法（约 177 行代码）
- 删除了以下硬编码演示数据：
  - 4 个账户（工商银行、招商银行、支付宝、建设银行）
  - 6 个客户（示例科技、示例贸易等）
  - 6 个供应商（示例云、示例网络等）
  - 4 个人员（张三、李四、王五、赵六）
  - 4 个项目
  - 38 条交易记录
  - 6 条应收记录
  - 7 条应付记录

**保留的必要数据**：
- ✅ 默认管理员用户（`admin` / `admin123`）- 系统必需
- ✅ 默认分类数据（16 个分类）- 系统必需的基础数据

#### 2. 测试数据（保留）✅

以下文件包含硬编码测试数据，属于正常的测试实践，**无需清理**：
- `TestDataBuilder.cs`（3 个文件）
- 各种 `*Tests.cs` 单元测试文件
- `appsettings.Testing.json`

#### 3. 配置文件安全问题 ⚠️

**需要改进的配置**（未在本次清理，建议后续优化）：
- `appsettings.json`：JWT Secret 和数据库密码明文存储
- 默认管理员密码 `admin123` 硬编码
- 建议：生产环境使用环境变量或密钥管理服务

### 前端代码

#### 测试数据（保留）✅

前端的硬编码数据主要集中在测试文件中，属于正常的测试实践：
- 36 个 `.test.ts` 文件包含 mock 数据
- `constants/enums.ts`：枚举选项（业务必需）
- `utils/dateShortcuts.ts`：日期快捷选项（功能必需）

**结论**：前端代码中没有需要清理的硬编码演示数据。

## 解决方案

### 1. 创建独立的 SQL 脚本

**文件**：`docs/02_Database/seed_demo_data.sql`

**功能**：
- 使用 PostgreSQL 存储过程创建演示数据
- 支持幂等性（检查数据是否已存在）
- 自动获取 admin 用户 ID 用于外键关联
- 包含完整的错误处理和日志输出

**使用方法**：
```bash
psql -U postgres -d finance_dev -f docs/02_Database/seed_demo_data.sql
```

### 2. 创建使用文档

**文件**：`docs/02_Database/README_DEMO_DATA.md`

**内容**：
- 演示数据脚本使用说明
- 前提条件和执行步骤
- 清理演示数据的方法
- 生产环境部署注意事项
- 故障排查指南

### 3. 修改代码逻辑

**修改文件**：
- `backend/FinanceApp.Infrastructure/Data/DbInitializer.cs`
  - 删除 `SeedDemoDataAsync` 方法
  - 移除 `seedDemoData` 参数
  - 添加注释指向 SQL 脚本

- `backend/FinanceApp.Api/Program.cs`
  - 简化 `DbInitializer.SeedAsync()` 调用
  - 移除环境判断逻辑

## 验证结果

### 编译测试
- ✅ 后端编译成功（0 个错误，9 个警告）
- ✅ 前端编译成功

### 代码行数对比
- **清理前**：`DbInitializer.cs` 429 行
- **清理后**：`DbInitializer.cs` 251 行
- **减少**：178 行（41.5%）

### 功能验证
- ✅ 系统启动时仍会创建默认管理员和分类
- ✅ 演示数据通过独立 SQL 脚本创建
- ✅ 生产环境不会自动创建演示数据

## 最佳实践建议

### 1. 数据初始化分层

**系统必需数据**（代码中保留）：
- 默认管理员用户
- 系统基础配置
- 必需的枚举/分类数据

**演示/测试数据**（独立脚本）：
- 示例账户、客户、供应商
- 示例交易记录
- 测试用的项目数据

### 2. 环境隔离

- **开发环境**：可使用演示数据脚本
- **测试环境**：使用专门的测试数据脚本
- **生产环境**：只创建系统必需数据

### 3. 配置安全

**建议改进**（后续任务）：
- JWT Secret 使用环境变量
- 数据库密码使用密钥管理服务
- 首次登录强制修改默认密码

### 4. 文档维护

- ✅ 创建了演示数据使用文档
- ✅ 添加了清理数据的方法
- ✅ 提供了故障排查指南

## 总结

本次清理工作：
1. ✅ 完全移除了代码中的硬编码演示数据
2. ✅ 创建了独立的 SQL 初始化脚本
3. ✅ 编写了完整的使用文档
4. ✅ 保留了系统必需的基础数据
5. ✅ 保留了测试代码中的 mock 数据
6. ✅ 验证了编译和功能正常

**代码质量提升**：
- 代码更简洁（减少 41.5% 的代码行数）
- 职责更清晰（代码只负责系统初始化，演示数据由脚本管理）
- 更易维护（演示数据可独立修改，无需重新编译）
- 更安全（生产环境不会意外创建演示数据）

## 后续建议

1. **配置安全加固**：将敏感配置移至环境变量
2. **密码策略**：首次登录强制修改默认密码
3. **数据导入工具**：开发 Excel/CSV 批量导入功能
4. **测试数据管理**：为不同测试场景创建专门的数据脚本
