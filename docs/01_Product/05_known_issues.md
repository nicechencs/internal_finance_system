# 已知问题

状态：Active  
适用对象：产品 / 开发 / 测试 / 运维  
事实源级别：Primary  
最后核对日期：2026-08-16  
代码依据：`backend/FinanceApp.Application/Modules/FinanceSettlement/Services/LinkService.cs`、`frontend/src/features/transactions/pages/UnallocatedTransactionsPage.vue`、`frontend/src/features/system/pages/AuditLogListPage.vue`

## KI-001 先导入流水后维护基础数据，默认不会自动回填全部关联

影响：

- 历史交易不会因为后来新增客户、供应商、项目而自动完成全部关联。

当前状态：

- 限制仍存在。
- 当前已有一键关联、批量智能关联等人工 / 半自动工具。

建议：

- 优先先维护基础数据，再导入流水。

## KI-002 对方名称重名时存在匹配歧义

影响：

- 自动匹配可能关联错误对象，或需要人工确认。

建议：

- 录入时增加区分标识。
- 结合账户信息与名称一起核查。

## KI-003 账户模块中“开户行 / 银行名称”命名仍不完全统一

影响：

- UI 一致性不足，用户理解成本偏高。

建议：

- 统一字段展示文案，并在所有列表、详情、表单、筛选项中同步。

## KI-004 列表排序能力尚未完全统一

影响：

- 部分列表已支持排序，但仍不是所有模块都实现一致。

当前状态：

- 定期存款列表已支持排序。
- 审计日志列表仍未看到排序交互。

建议：

- 后续按模块逐步补齐和统一排序交互。

## KI-005 凭证号能力仍待补齐

当前状态：

- `description` 与 `memo` 已拆分。
- `voucher_no` 仍未落地。

建议：

- 业务上如果需要凭证追溯，建议进入近期开发计划。

## KI-006 规则不会自动对历史数据全量重跑

当前状态：

- 系统已支持手动规则重跑预览与确认。
- 仍不支持“新增规则后自动全量重跑”。

建议：

- 把“自动”和“手动可执行”明确区分给使用者。

## KI-007 待分配交易处理页仍未完整闭环

当前状态：

- 2026-08-16 已闭环：列表按 `Unallocated/PartiallyAllocated` 筛选并排除转账；支持关联已有未结清单据、创建新单并核销、批量创建并核销。
- 同一笔交易对同一张单据仍只能核销一次；创建+核销两步失败时保留单据，需到详情继续登记。

建议：

- 批量创建要求收入必须有项目、所有单据必须有对方；缺字段的交易请走单笔「处理」。

## KI-008 账户作为关联对象的 LinkService 分支暂未支持

当前状态：

- 代码中账户关联分支会返回“账户关联功能暂不支持”。

建议：

- 如业务需要账户级关联，应补齐后端分支、前端入口和回归测试。
